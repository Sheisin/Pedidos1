# Copie este arquivo para ".env" e ajuste se necessário.
# Esses valores são usados pelo docker-compose.yml para configurar o PostgreSQL
# e, por consequência, a string de conexão da aplicação (SPRING_DATASOURCE_URL).

POSTGRES_DB=pedidos
POSTGRES_USER=pedidos
POSTGRES_PASSWORD=pedidos
